import { styled } from "styled-components";

const Icon = styled.span`
    margin: 0 12px;

    & > img {
        width: 50px;
        height: 50px;
        border-radius: 12px;
    }
`;

export {
    Icon,
}